# Android build configuration
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Required because the app uses AndroidX libraries such as androidx.core and androidx.activity.
android.useAndroidX=true

# Keep generated resources smaller and builds cleaner.
android.nonTransitiveRClass=true
